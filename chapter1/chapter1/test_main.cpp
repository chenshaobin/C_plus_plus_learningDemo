#include <iostream>
int main()
{
	/*
		* 标准库定义的所有名字都在命名空间std中
		* ::为作用域运算符
		* 同一个项目中的多个源程序，在运行时，应该将不参与生成的文件的属性设置为 从生成中排除
	*/
	//std::cout << "Hello, World!" << std::endl;

	//计算输入的两个数之和
	/*
		std::cout << "Enter two numbers:" << std::endl;
		int v1 = 0, v2 = 0;
		std::cin  >> v1 >> v2;
		std::cout << "The sum of " << v1 << " and " << v2
			<< " is " << v1 + v2 << std::endl;
	*/
	
	// 计算两个数之积
		/*
			std::cout << "Enter two numbers:" << std::endl;
			int v1 = 0, v2 = 0;
			std::cin >> v1 >> v2;
			std::cout << "The product of " << v1 << "and " << v2
				<< " is " << v1 * v2 << std::endl;
		*/

	std::cout << /* "*/" /* "/*" */;
	return 0;
}